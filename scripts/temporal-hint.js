console.log(`
Temporal (Docker) is up:
  gRPC (app / worker):  localhost:7233
  Web UI (browser):     http://localhost:8080  → choose namespace "default"
  Logs:                 npm run temporal:logs
  Stop:                 npm run temporal:down
`);
